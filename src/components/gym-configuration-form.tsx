"use client";

import { useActionState } from "react";
import { createGymAction, updateGymAction, uploadGymLogoAction } from "@/features/gyms/actions";
import { initialGymActionState } from "@/features/gyms/state";

export type GymFormValues = Readonly<{
  name?: string; slug?: string; addressLine1?: string; addressLine2?: string; city?: string; postcode?: string;
  countryCode?: string; timezone?: string; contactEmail?: string; contactPhone?: string; disciplines?: string[];
  openingHoursText?: string; publicJoinRequestsEnabled?: boolean; primaryColour?: string; accentColour?: string;
  backgroundColour?: string; welcomeMessage?: string;
}>;

const inputClass = "mt-2 min-h-11 w-full rounded-lg border border-[var(--border)] bg-white px-3 font-normal";
const disciplines = [["bouldering", "Bouldering"], ["sport", "Sport"], ["trad", "Trad"], ["speed", "Speed"], ["training", "Training"]] as const;

function Result({ state }: { state: typeof initialGymActionState }) {
  return state.message ? <p aria-live="polite" className={state.status === "error" ? "text-sm text-red-700" : "text-sm text-green-800"}>{state.message}</p> : null;
}

export function GymConfigurationForm({ mode, values = {} }: { mode: "create" | "edit"; values?: GymFormValues }) {
  const [state, action, pending] = useActionState(mode === "create" ? createGymAction : updateGymAction, initialGymActionState);
  return <form action={action} className="space-y-8"><input name="currentSlug" type="hidden" value={values.slug ?? ""} /><fieldset className="grid gap-4 rounded-2xl border border-[var(--border)] bg-white p-5 md:grid-cols-2"><legend className="px-2 text-lg font-black">1. Gym details</legend><label className="text-sm font-semibold">Gym name<input className={inputClass} defaultValue={values.name} name="name" required /></label><label className="text-sm font-semibold">URL slug<input className={inputClass} defaultValue={values.slug} name="slug" required /></label><label className="text-sm font-semibold md:col-span-2">Address<input className={inputClass} defaultValue={values.addressLine1} name="addressLine1" required /></label><label className="text-sm font-semibold">Address line 2<input className={inputClass} defaultValue={values.addressLine2} name="addressLine2" /></label><label className="text-sm font-semibold">City<input className={inputClass} defaultValue={values.city} name="city" required /></label><label className="text-sm font-semibold">Postcode<input className={inputClass} defaultValue={values.postcode} name="postcode" required /></label><label className="text-sm font-semibold">Country code<input className={inputClass} defaultValue={values.countryCode ?? "GB"} maxLength={2} name="countryCode" required /></label><label className="text-sm font-semibold">Timezone<input className={inputClass} defaultValue={values.timezone ?? "Europe/London"} name="timezone" required /></label><label className="text-sm font-semibold">Contact email<input className={inputClass} defaultValue={values.contactEmail} name="contactEmail" required type="email" /></label><label className="text-sm font-semibold">Contact phone<input className={inputClass} defaultValue={values.contactPhone} name="contactPhone" /></label><div className="md:col-span-2"><p className="text-sm font-semibold">Climbing disciplines</p><div className="mt-2 flex flex-wrap gap-3">{disciplines.map(([value,label]) => <label className="flex items-center gap-2 rounded-lg border border-[var(--border)] px-3 py-2 text-sm" key={value}><input defaultChecked={values.disciplines?.includes(value)} name="disciplines" type="checkbox" value={value} />{label}</label>)}</div></div><label className="text-sm font-semibold md:col-span-2">Opening hours<textarea className={`${inputClass} min-h-28 py-3`} defaultValue={values.openingHoursText} name="openingHoursText" required /></label><label className="flex items-center gap-3 text-sm font-semibold md:col-span-2"><input defaultChecked={values.publicJoinRequestsEnabled} name="publicJoinRequestsEnabled" type="checkbox" />Allow public membership requests</label></fieldset><fieldset className="grid gap-4 rounded-2xl border border-[var(--border)] bg-white p-5 md:grid-cols-3"><legend className="px-2 text-lg font-black">2. Accessible branding</legend><label className="text-sm font-semibold">Primary colour<input className={`${inputClass} p-1`} defaultValue={values.primaryColour ?? "#17211B"} name="primaryColour" type="color" /></label><label className="text-sm font-semibold">Accent colour<input className={`${inputClass} p-1`} defaultValue={values.accentColour ?? "#D9FF45"} name="accentColour" type="color" /></label><label className="text-sm font-semibold">Background colour<input className={`${inputClass} p-1`} defaultValue={values.backgroundColour ?? "#F7F7F2"} name="backgroundColour" type="color" /></label><label className="text-sm font-semibold md:col-span-3">Welcome message<textarea className={`${inputClass} min-h-24 py-3`} defaultValue={values.welcomeMessage} name="welcomeMessage" /></label><p className="text-xs text-[var(--muted)] md:col-span-3">Primary text must maintain at least 4.5:1 contrast against the background.</p></fieldset><div className="flex items-center gap-4"><button className="min-h-11 rounded-full bg-[var(--foreground)] px-6 text-sm font-bold text-white disabled:opacity-60" disabled={pending}>{pending ? "Saving…" : mode === "create" ? "Create gym" : "Save settings"}</button><Result state={state} /></div></form>;
}

export function GymLogoForm({ gymSlug }: { gymSlug: string }) {
  const [state, action, pending] = useActionState(uploadGymLogoAction, initialGymActionState);
  return <form action={action} className="rounded-2xl border border-[var(--border)] bg-white p-5"><input name="gymSlug" type="hidden" value={gymSlug} /><h2 className="text-lg font-black">3. Gym logo</h2><p className="mt-2 text-sm text-[var(--muted)]">PNG, JPEG, or WebP. Maximum 2 MB. Images are stored in a private tenant-scoped bucket.</p><input accept="image/png,image/jpeg,image/webp" className="mt-4 block text-sm" name="logo" required type="file" /><button className="mt-4 min-h-11 rounded-full bg-[var(--foreground)] px-5 text-sm font-bold text-white disabled:opacity-60" disabled={pending}>{pending ? "Uploading…" : "Upload logo"}</button><div className="mt-3"><Result state={state} /></div></form>;
}
