"use client";
import{useActionState}from"react";import{checkInMemberTokenAction,manualMemberCheckInAction,verifyMemberTokenAction}from"@/features/checkins/actions";import{initialMemberCheckInState}from"@/features/checkins/state";
export function MemberTokenVerifier({gymSlug}:{gymSlug:string}){const[verify,verifyAction,pending]=useActionState(verifyMemberTokenAction,initialMemberCheckInState);const[checkin,checkinAction,checking]=useActionState(checkInMemberTokenAction,initialMemberCheckInState);return <section className="rounded-2xl border border-[var(--border)] bg-white p-5"><h2 className="text-xl font-black">Scan member QR</h2><form action={verifyAction} className="mt-4 flex flex-col gap-3 sm:flex-row"><input name="gymSlug" type="hidden" value={gymSlug}/><input autoComplete="off" className="min-h-11 flex-1 rounded-lg border p-3 font-mono text-sm" name="reference" placeholder="CRUXMEM1.…" required/><button className="rounded-full bg-black px-5 text-sm font-bold text-white" disabled={pending}>{pending?"Checking…":"Verify"}</button></form>{verify.message?<p className={`mt-3 text-sm ${verify.status==="error"?"text-red-700":"text-emerald-700"}`}>{verify.message}</p>:null}{verify.result?.found?<div className="mt-4 rounded-xl bg-stone-50 p-4"><h3 className="text-xl font-black">{verify.result.memberName}</h3><p className="mt-2 text-sm">Membership: {verify.result.membershipStatus} ({verify.result.membershipSource} source)</p><p className="text-sm">Waivers: {verify.result.waiversComplete?"complete":"incomplete"} · Token: {verify.result.tokenStatus}</p><form action={checkinAction} className="mt-4"><input name="gymSlug" type="hidden" value={gymSlug}/><input name="reference" type="hidden" value={verify.reference}/><button className="min-h-11 rounded-full bg-emerald-700 px-5 text-sm font-bold text-white disabled:opacity-50" disabled={checking||verify.result.tokenStatus!=="valid"||!verify.result.waiversComplete}>{checking?"Recording…":"Check in member"}</button></form>{checkin.message?<p className={`mt-3 text-sm ${checkin.status==="error"?"text-red-700":"text-emerald-700"}`}>{checkin.message}</p>:null}</div>:null}</section>}
export function ManualMemberCheckIn({gymSlug,members}:{gymSlug:string;members:{membershipId:string;displayName:string}[]}){const[state,action,pending]=useActionState(manualMemberCheckInAction,initialMemberCheckInState);return <section className="rounded-2xl border border-[var(--border)] bg-white p-5"><h2 className="text-xl font-black">Manual fallback</h2><p className="mt-2 text-sm text-[var(--muted)]">Use after confirming the member’s identity. The same waiver and active-membership checks still apply.</p><form action={action} className="mt-4 flex flex-col gap-3 sm:flex-row"><input name="gymSlug" type="hidden" value={gymSlug}/><select className="min-h-11 flex-1 rounded-lg border p-3" name="membershipId" required><option value="">Select matching member</option>{members.map((member)=><option key={member.membershipId} value={member.membershipId}>{member.displayName}</option>)}</select><button className="rounded-full bg-black px-5 text-sm font-bold text-white" disabled={pending}>{pending?"Recording…":"Manual check-in"}</button></form>{state.message?<p className={`mt-3 text-sm ${state.status==="error"?"text-red-700":"text-emerald-700"}`}>{state.message}</p>:null}</section>}
