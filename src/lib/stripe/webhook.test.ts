import Stripe from"stripe";import{describe,expect,it}from"vitest";import{verifyStripeEventWithClient}from"./webhook";
describe("Stripe webhook verification",()=>{const stripe=new Stripe("sk_test_fixture");const secret="whsec_fixture_secret";it("accepts a signed fixture and rejects tampering",()=>{const payload=JSON.stringify({id:"evt_fixture",object:"event",type:"customer.subscription.updated",livemode:false,data:{object:{id:"sub_fixture"}}}),signature=stripe.webhooks.generateTestHeaderString({payload,secret,timestamp:Math.floor(Date.now()/1000)});expect(verifyStripeEventWithClient(payload,signature,secret,stripe).id).toBe("evt_fixture");expect(()=>verifyStripeEventWithClient(`${payload} `,signature,secret,stripe)).toThrow();});});
