import{NextRequest}from"next/server";import{z}from"zod";import{requireActiveGymContext}from"@/lib/server/gym-context";import{createServerComponentSupabaseClient}from"@/lib/supabase/server";
const schema=z.object({from:z.iso.date(),to:z.iso.date()});function cell(value:unknown){return`"${String(value??"").replaceAll('"','""')}"`;}
export async function GET(request:NextRequest,{params}:{params:Promise<{gymSlug:string}>}){const{gymSlug}=await params,{gym}=await requireActiveGymContext({gymSlug,allowedRoles:["owner","staff"]}),url=new URL(request.url),parsed=schema.safeParse({from:url.searchParams.get("from"),to:url.searchParams.get("to")});if(!parsed.success)return new Response("Invalid date range",{status:400});const s=await createServerComponentSupabaseClient(),{data,error}=await s.rpc("get_gym_operational_analytics",{target_gym_id:gym.id,date_from:parsed.data.from,date_to:parsed.data.to});if(error)return new Response("Forbidden",{status:403});const csv=["period,metric_key,metric_label,metric_value,definition",...(data??[]).map(row=>[row.period,row.metric_key,row.metric_label,row.metric_value,row.definition].map(cell).join(","))].join("\n");return new Response(csv,{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":`attachment; filename="gym-analytics-${parsed.data.from}-${parsed.data.to}.csv"`,"cache-control":"private, no-store"}})}
