import Image from"next/image";import Link from"next/link";import{requireActiveGymContext}from"@/lib/server/gym-context";import{createServerComponentSupabaseClient}from"@/lib/supabase/server";
export default async function EventsPage({params,searchParams}:{params:Promise<{gymSlug:string}>;searchParams:Promise<{view?:string}>}){const[{gymSlug},search]=await Promise.all([params,searchParams]);const{gym}=await requireActiveGymContext({gymSlug});const supabase=await createServerComponentSupabaseClient();const[{data:settings},{data:events}]=await Promise.all([supabase.from("gyms").select("timezone").eq("id",gym.id).single(),supabase.from("events").select("id,event_type,title,description,location,starts_at,ends_at,capacity,image_path").eq("gym_id",gym.id).eq("status","published").gte("ends_at",new Date().toISOString()).order("starts_at")]);const timezone=settings?.timezone??"Europe/London",view=search.view==="calendar"?"calendar":"list";const cards=await Promise.all((events??[]).map(async(event)=>({...event,imageUrl:event.image_path?(await supabase.storage.from("event-images").createSignedUrl(event.image_path,3600)).data?.signedUrl:null})));const grouped=new Map<string,typeof cards>();for(const event of cards){const day=new Intl.DateTimeFormat("en-CA",{timeZone:timezone,year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date(event.starts_at));grouped.set(day,[...(grouped.get(day)??[]),event]);}const base=`/g/${gym.slug}/app/events`;return <div className="mx-auto max-w-6xl"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-sm font-bold uppercase tracking-[.2em] text-[var(--muted)]">Events</p><h1 className="mt-3 text-4xl font-black">What’s happening</h1></div><div className="flex rounded-full border bg-white p-1"><Link className={`rounded-full px-4 py-2 text-sm font-bold ${view==="list"?"bg-black text-white":""}`} href={base}>List</Link><Link className={`rounded-full px-4 py-2 text-sm font-bold ${view==="calendar"?"bg-black text-white":""}`} href={`${base}?view=calendar`}>Calendar</Link></div></div>{view==="calendar"?<div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{[...grouped].map(([day,items])=><section className="rounded-2xl border border-[var(--border)] bg-white p-5" key={day}><h2 className="font-black">{new Intl.DateTimeFormat("en-GB",{timeZone:timezone,dateStyle:"full"}).format(new Date(`${day}T12:00:00Z`))}</h2><ul className="mt-3 space-y-2">{items.map((event)=><li key={event.id}><Link className="block rounded-lg bg-stone-50 p-3" href={`${base}/${event.id}`}><strong>{event.title}</strong><span className="block text-xs">{new Intl.DateTimeFormat("en-GB",{timeZone:timezone,timeStyle:"short"}).format(new Date(event.starts_at))} · {event.location??"Location TBC"}</span></Link></li>)}</ul></section>)}</div>:<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{cards.map((event)=><article className="overflow-hidden rounded-2xl border border-[var(--border)] bg-white" key={event.id}>{event.imageUrl?<Image alt="" className="aspect-video w-full object-cover" height={600} src={event.imageUrl} unoptimized width={1000}/>:null}<div className="p-5"><p className="text-xs font-bold uppercase text-[var(--muted)]">{event.event_type}</p><h2 className="mt-2 text-xl font-black">{event.title}</h2><p className="mt-2 text-sm">{new Intl.DateTimeFormat("en-GB",{timeZone:timezone,dateStyle:"medium",timeStyle:"short"}).format(new Date(event.starts_at))}</p><p className="mt-1 text-sm text-[var(--muted)]">{event.location??"Location TBC"}</p><Link className="mt-4 inline-flex min-h-11 items-center font-bold underline" href={`${base}/${event.id}`}>View and book</Link></div></article>)}</div>}</div>}
