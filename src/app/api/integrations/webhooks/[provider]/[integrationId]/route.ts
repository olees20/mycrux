import{createHmac,timingSafeEqual}from"node:crypto";import{z}from"zod";import{getServerEnvironment}from"@/env/server";import{providerByKey}from"@/lib/integrations/providers";import{privilegedAccess}from"@/lib/supabase/admin";import type{Json}from"@/lib/supabase/database.types";
const paramsSchema=z.object({provider:z.string().regex(/^[a-z][a-z0-9_.-]*$/),integrationId:z.uuid()});
export async function POST(request:Request,{params}:{params:Promise<{provider:string;integrationId:string}>}){const parsed=paramsSchema.safeParse(await params),secret=getServerEnvironment().INTEGRATION_WEBHOOK_SECRET;if(!parsed.success)return new Response("Not found",{status:404});const adapter=providerByKey(parsed.data.provider);if(!adapter||!adapter.available)return new Response("Adapter disabled",{status:503});if(!secret)return new Response("Webhook verification unavailable",{status:503});const contentLength=Number(request.headers.get("content-length")??0);if(contentLength>1048576)return new Response("Payload too large",{status:413});const raw=await request.text();if(Buffer.byteLength(raw)>1048576)return new Response("Payload too large",{status:413});const supplied=request.headers.get("x-crux-signature")??"",expected=createHmac("sha256",secret).update(raw).digest("hex"),left=Buffer.from(supplied),right=Buffer.from(expected);if(left.length!==right.length||!timingSafeEqual(left,right))return new Response("Invalid signature",{status:401});const eventKey=request.headers.get("x-crux-event-id");if(!eventKey||eventKey.length>255)return new Response("Missing event id",{status:400});const connection=await privilegedAccess.getIntegrationForWebhook(parsed.data.integrationId,parsed.data.provider);if(!connection||!(["configured","active"]as string[]).includes(connection.status))return new Response("Integration unavailable",{status:404});let payload:unknown;try{payload=JSON.parse(raw)}catch{return new Response("Invalid JSON",{status:400})}if(!payload||typeof payload!=="object"||Array.isArray(payload))return new Response("Invalid payload",{status:400});await privilegedAccess.ingestIntegrationDelivery(connection.id,connection.provider_key,eventKey,payload as Json);return new Response(null,{status:202})}
